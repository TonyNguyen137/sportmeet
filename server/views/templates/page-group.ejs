<%- include("../partials/header", { variant: "group-page" }) %>

<main>
	<div class="container mx-auto px-[12px] py-[32px]">
		<section class="pane">
			<button
				type="button"
				onclick="
					if (window.history.length > 1) {
						window.history.back();
					} else {
						window.location.href = '/me#view-groups';
					}
				"
				class="text-accent-blue hover:text-accent-dark-blue mb-4 inline-flex cursor-pointer items-center gap-2 text-sm font-semibold">
				<span aria-hidden="true"><-</span>
				Zurück zu Gruppen
			</button>
			<h1 class="text-h1 text-dark font-bold">Hallo von <%= group.name %></h1>
		</section>
	</div>
</main>

<%- include("../partials/footer") %>
